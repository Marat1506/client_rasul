# Настройка фронтенда для работы с удаленным бэкендом

## ✅ Что было сделано

1. **Создан файл `.env.local`** с настройками:
   ```env
   NEXT_PUBLIC_BASE_URL=http://31.57.228.116
   NEXT_PUBLIC_SOCKET_URL=http://31.57.228.116
   ```

2. **Проверена доступность бэкенда:**
   - Бэкенд доступен по адресу `http://31.57.228.116`
   - Health check возвращает `200 OK`
   - CORS настроен правильно (разрешены все origins)

## 🔄 Следующие шаги

### 1. Перезапустите dev сервер

**Важно:** Next.js загружает переменные окружения только при запуске. После создания/изменения `.env.local` нужно перезапустить сервер.

```bash
# Остановите текущий сервер (нажмите Ctrl+C)
# Затем запустите снова:
cd client
npm run dev
# или
yarn dev
```

### 2. Проверьте подключение

После перезапуска:

1. **Откройте браузер:**
   - Откройте консоль разработчика (F12)
   - Перейдите на вкладку "Network"

2. **Проверьте запросы:**
   - Все запросы должны идти на `http://31.57.228.116` вместо `localhost:3001`
   - Запросы должны возвращать `200 OK` (не `Network Error`)

3. **Проверьте консоль:**
   - Не должно быть ошибок "Network Error"
   - WebSocket должен подключаться к `http://31.57.228.116`

## 🔍 Troubleshooting

### Ошибка "Network Error" все еще появляется

1. **Проверьте файл `.env.local`:**
   ```bash
   cat client/.env.local
   ```
   Должно быть:
   ```
   NEXT_PUBLIC_BASE_URL=http://31.57.228.116
   NEXT_PUBLIC_SOCKET_URL=http://31.57.228.116
   ```

2. **Убедитесь, что сервер перезапущен:**
   - Остановите сервер (Ctrl+C)
   - Запустите снова (`npm run dev`)

3. **Проверьте доступность бэкенда:**
   ```bash
   curl http://31.57.228.116/api/v1/health
   ```
   Должен вернуть: `{"status":"ok","timestamp":"..."}`

4. **Проверьте в браузере:**
   - Откройте DevTools (F12)
   - Перейдите на вкладку "Network"
   - Обновите страницу
   - Проверьте, на какой URL идут запросы

### WebSocket не подключается

1. **Проверьте Socket.IO на бэкенде:**
   - Убедитесь, что Socket.IO сервер запущен
   - Проверьте настройки CORS для Socket.IO

2. **Проверьте переменную окружения:**
   ```bash
   echo $NEXT_PUBLIC_SOCKET_URL
   # или в браузере:
   console.log(process.env.NEXT_PUBLIC_SOCKET_URL)
   ```

### CORS ошибки

Если получаете CORS ошибки:
- Бэкенд настроен на разрешение всех origins (`origin: true`)
- Проверьте, что Nginx не блокирует запросы
- Проверьте настройки бэкенда

## 📝 Примечания

- Файл `.env.local` находится в `.gitignore`, поэтому не будет закоммичен в git
- Для продакшена (Vercel) нужно будет настроить переменные окружения в настройках Vercel
- Если бэкенд будет доступен по домену, обновите `NEXT_PUBLIC_BASE_URL` на домен

